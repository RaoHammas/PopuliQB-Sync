<Window
    x:Class="PopuliQB_Tool.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PopuliQB_Tool"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Populi to QuickBooks Sync"
    Width="800"
    Height="450"
    mc:Ignorable="d">

    <Grid>
        <Grid.Background>
            <SolidColorBrush Opacity="0.2" Color="LightGray" />
        </Grid.Background>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox
                    Grid.Column="0"
                    MinWidth="250"
                    Margin="0,0,0,0"
                    materialDesign:HintAssist.FloatingScale="1"
                    materialDesign:HintAssist.Hint="QuickBooks Desktop Company"
                    Style="{StaticResource MaterialDesignOutlinedTextBox}" />

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <DatePicker
                        MinWidth="180"
                        Margin="10,0,0,0"
                        materialDesign:HintAssist.FloatingScale="1"
                        materialDesign:HintAssist.Hint="Start Transaction Date"
                        Style="{StaticResource MaterialDesignOutlinedDatePicker}" />
                </StackPanel>
            </Grid>
            <StackPanel
                Grid.Row="1"
                Margin="0,20,0,0"
                Orientation="Horizontal">
                <Button
                    Height="40"
                    Padding="10"
                    Content="Connect to QB" />
                <Button
                    Height="40"
                    Margin="10,0,0,0"
                    Padding="10"
                    Content="Start Populi to QB Sync" />
            </StackPanel>

            <Grid Grid.Row="2" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="20" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBox
                    Grid.Column="0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    materialDesign:HintAssist.FloatingScale="1"
                    materialDesign:HintAssist.Hint="Sync Status"
                    AcceptsReturn="True"
                    Background="Beige"
                    IsReadOnly="True"
                    Style="{StaticResource MaterialDesignOutlinedTextBox}"
                    TextWrapping="Wrap"
                    VerticalScrollBarVisibility="Auto" />

                <TextBox
                    Grid.Column="2"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    materialDesign:HintAssist.FloatingScale="1"
                    materialDesign:HintAssist.Hint="Statistics"
                    AcceptsReturn="True"
                    Background="Azure"
                    IsReadOnly="True"
                    Style="{StaticResource MaterialDesignOutlinedTextBox}"
                    TextWrapping="Wrap"
                    VerticalScrollBarVisibility="Auto" />
            </Grid>
        </Grid>
    </Grid>
</Window>